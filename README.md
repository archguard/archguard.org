# ArchGuard 官网

## Development

setup:

```
bundle install
```

change `_config.yml` theme to: `theme: just-the-docs` for local 

run:

```
bundle exec jekyll serve
```

with Rubyenv

```
rbenv install 3.1.0
gem install bundle
```

